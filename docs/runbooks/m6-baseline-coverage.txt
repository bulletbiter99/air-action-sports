M6 BASELINE COVERAGE — snapshot at milestone close (2026-05-27)
================================================================

Source-of-truth: main at the B11 merge SHA (this commit).

This file is the cumulative coverage reference for M6. Mirrors the
docs/runbooks/m{1,2,3,4,5,5.5}-baseline-coverage.* files for prior
milestones.

────────────────────────────────────────────────────────────────────
TEST + LINT + BUILD STATE
────────────────────────────────────────────────────────────────────

Tests:           2292 passed across 184 files (npm test)
Test runner:     Vitest 2.1.x, Node 20
Lint:            0 errors / ~462 warnings (npm run lint)
                 Warnings are advisory: react-refresh/
                 only-export-components on JSX pure-helper exports +
                 a handful of no-unused-vars false-positives on JSX
                 components that ESLint can't track usage of without
                 the react/jsx-uses-vars plugin (not in this repo's
                 eslint.config.js).
Build:           clean (~255-290 ms)

Test growth across M6:
  M5.5 close:    1997 / 161   (baseline at start of M6)
  Post-M5.5 fix: 2073 / 168   (intervening admin staff suite work)
  M6 B0-B2:      2135 / 173   (vendor package templates)
  M6 B3:         2175 / 176   (email template draft state — schema + worker)
  M6 B4:         2212 / 178   (email template admin UI + preview-w/-real-data)
  M6 B5:         2231 / 180   (Stripe setup_future_usage)
  M6 B6:         2251 / 182   (charge.dispute.created consumer)
  M6 B10:        2251 / 182   (booking_confirmation template content)
  M6 B7:         2279 / 184   (damage charge Option A activation)
  M6 B8:         2279 / 184   (admin UI for Charge card; no new tests)
  M6 B9:         2292 / 184   (admin remove-saved-PM)
  M6 B11:        2292 / 184   (closing runbooks; docs only)
  ─────────────────
  M6 total:      +295 tests / +23 files vs M5.5 close
                 +157 tests / +16 files vs post-M5.5 admin-suite work

Group A + Group B regression: 138 / 138 preserved throughout the
milestone (138 expanded to 149/149 with B6's 11 new dispute-handler +
unhandled-event-type tests; the original 138 unchanged).

────────────────────────────────────────────────────────────────────
D1 MIGRATIONS APPLIED DURING M6
────────────────────────────────────────────────────────────────────

3 migrations, all applied to remote:

  0056_email_templates_status.sql              (B3; 2026-05-26)
    - ALTER TABLE email_templates ADD COLUMN status
    - Backfilled 33 existing rows to status='published'

  0057_dispute_received_email_template.sql    (B6; 2026-05-26)
    - INSERT INTO email_templates (tpl_dispute_received) — admin alert
      template for charge.dispute.created webhook

  0058_booking_confirmation_charges_notice.sql (B10; 2026-05-26)
    - UPDATE booking_confirmation template's body_html + body_text to
      add the "Heads-up — Additional Charges May Apply" section.
      Pure UPDATE on existing seed row — no schema change.

All additive / content-only. No D1 quirks hit. No table rebuilds.

────────────────────────────────────────────────────────────────────
DEPLOY VERSION SEQUENCE
────────────────────────────────────────────────────────────────────

Each batch's merge to main auto-triggers Workers Builds:

  B0:         (M5.5 close baseline) — pre-M6
  B0/B1/B2:   a6c147db-8299-45e8-82ab-d0ee1e0ac115  (vendor templates)
  B3:         (no separate deploy — merged together with B4)
  B4:         09c58ed1-c5ed-41d2-bfc9-1daf72df973e
  B5:         ba6545c2-f324-4ca4-8fdd-c5bfc2faa91a
  B6:         518af64a-58c3-476a-97a6-4a1721812443
  B10:        954964c3-56f3-4f08-9c97-47cd54b85c35
  B7:         (deployed with B8)
  B8:         c02d00fc-b7d6-4f96-abbd-0d5bb579bdbb
  B9:         e8372102-d9e3-4799-9678-261912192ab1
  B11:        <closing — see git log>

────────────────────────────────────────────────────────────────────
GATED PATHS NEWLY ADDED OR EXTENDED IN M6
────────────────────────────────────────────────────────────────────

In scripts/test-gate-mapping.json:

  worker/lib/stripe.js — extended with:
    - createCheckoutSession (M6 B5) + setupFutureUsage option
    - retrievePaymentIntent (M6 B7)
    - chargeOffSession (M6 B7)
    - detachPaymentMethod (M6 B9)
    Tests: stripe-setup-future-usage / stripe-off-session-charge /
           stripe-detach-pm

  worker/routes/webhooks.js — extended with:
    - handleDisputeCreated (M6 B6)
    Test: dispute-created-handler

  worker/routes/bookings.js — promoted from `uncovered`:
    - POST /api/bookings/checkout
    Test: checkout-setup-future-usage

  worker/routes/admin/bookings.js — extended with:
    - POST /:id/detach-saved-pm (M6 B9)
    Test: detach-saved-pm

  worker/lib/bookingCharges.js — promoted from `uncovered`:
    Test: route.test.js + charge-card.test.js

  worker/routes/admin/bookingCharges.js — promoted from `uncovered`:
    Test: route.test.js + charge-card.test.js

  worker/lib/templates.js — gate for loadTemplate draft filter (M6 B3)
    Test: templates-load-status-filter

  worker/lib/emailTemplates.js — NEW (M6 B3)
    Test: emailTemplates-helpers

  worker/lib/emailTemplatePreview.js — NEW (M6 B4)
    Test: emailTemplatePreview

  worker/routes/admin/emailTemplates.js — gated (M6 B3/B4):
    Tests: email-templates-status / email-templates-preview-with-data

  worker/lib/vendorPackageTemplates.js — gated (M6 B2)
    Test: vendor-package-templates

  worker/routes/admin/vendorPackageTemplates.js — gated (M6 B1/B2)
    Tests: list-route / edit-route / clone-route

────────────────────────────────────────────────────────────────────
EMAIL TEMPLATES AT M6 CLOSE
────────────────────────────────────────────────────────────────────

34 templates in production (33 at M5.5 close + dispute_received in B6).
1 template's content updated (booking_confirmation in B10).

All templates conform to Lesson #7 schema (id TEXT PRIMARY KEY +
created_at INTEGER NOT NULL).

────────────────────────────────────────────────────────────────────
CRITICAL DNT SURFACES TOUCHED IN M6
────────────────────────────────────────────────────────────────────

Touched additively (Group A + B regression intact):

  worker/lib/stripe.js
    - createCheckoutSession gained optional setupFutureUsage (B5)
    - retrievePaymentIntent added (B7)
    - chargeOffSession added (B7)
    - detachPaymentMethod added (B9)
    verifyWebhookSignature byte-equivalent throughout — Group B 59/59.

  worker/routes/bookings.js
    - POST /checkout passes setupFutureUsage='off_session' (B5)
    Existing pricing math + line items + URLs preserved exactly.
    Group A 79/79.

  worker/routes/webhooks.js
    - New else-if branch for charge.dispute.created (B6)
    handleCheckoutCompleted + signature-verify byte-equivalent. Group
    B handler 9/9 preserved.

  worker/routes/admin/bookings.js
    - POST /:id/detach-saved-pm appended (B9)
    Existing manual / refund / refund-external / resend handlers
    byte-equivalent.

  worker/lib/emailSender.js
    - sendDisputeAlert added (B6) — 10th named sender
    Original 9 DNT senders byte-equivalent.

  worker/lib/templates.js
    - loadTemplate gained { includeDrafts } option (B3) — surgical
      4-line change; backwards-compatible default behavior.

────────────────────────────────────────────────────────────────────
WHAT'S LEFT — OPERATOR-ONLY ITEMS
────────────────────────────────────────────────────────────────────

5 cutover items in docs/m6-operator-cutover-checklist.md:
  1. wrangler secret put STRIPE_SECRET_KEY (live)
  2. wrangler secret put STRIPE_WEBHOOK_SECRET (live)
  3. Configure live Stripe webhook endpoint
  4. Verify DMARC + SPF + DKIM DNS records
  5. $1 live e2e test

All M6 code is shipped + tested + on production sandbox. Live
verification of B5 (setup_future_usage), B6 (dispute consumer), B7
(off-session capture), and B9 (PM detach) happens after the operator
completes items 1-5.

────────────────────────────────────────────────────────────────────
SUMMARY
────────────────────────────────────────────────────────────────────

M6 = 13 batches (B0, B0-followup, B1, B2, B3, B4, B5, B6, B7, B8, B9,
B10, B11). All code merged + deployed. All tests passing. All Group
A + B regression preserved. 3 D1 migrations applied to remote.

The milestone closes with full sandbox-mode functionality:
  - Vendor package templates library + composer (B1/B2)
  - Email template draft state (B3/B4)
  - Stripe setup_future_usage on checkout (B5)
  - charge.dispute.created webhook consumer (B6)
  - Damage charge Option A (off-session capture) (B7/B8)
  - Admin remove-saved-PM (B9)
  - booking_confirmation "additional charges may apply" copy (B10)
  - Cumulative runbooks (B11)

Live verification of payment-flow features (B5/B6/B7/B9) is gated on
the operator-only cutover. Once that lands, M6 reaches full coverage.
